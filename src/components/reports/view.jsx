import R from "ramda";
import React, {PropTypes} from "react";

const Reports = (props) => (
  <div className="reports-view">
    Reports View
  </div>
);


Reports.propTypes = {
};

export default Reports;